import { Genre, ImageStyle } from "./types";

export const GENRE_OPTIONS = Object.values(Genre);
export const STYLE_OPTIONS = Object.values(ImageStyle);

export const DEFAULT_PAGE_COUNT_OPTIONS = [4, 6, 8, 10, 12, 16];

export const MOCK_MASCOT_IMAGE = "https://cdn-icons-png.flaticon.com/512/4712/4712009.png"; // A cute frog/sapu placeholder icon

export const DEFAULT_COLORS = {
  primary: "#16a34a", // sapu-600
  secondary: "#f0fdf4", // sapu-50
};
